# Example local mise configuration for development
# Copy this file to mise.local.toml and fill in your values
# mise.local.toml is gitignored and should not be committed

[env]
# Add per-developer environment overrides here as the memoir-server epic
# adds them (e.g. JWT_SECRET, LLM provider API keys).
